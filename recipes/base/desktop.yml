modules:
  - type: dnf
    install:
      skip-unavailable: true
      packages:
        - ntsync-autoload
    remove:
      packages:
        - firefox
        - firefox-langpacks

  - type: justfiles
    validate: true
    install: true
    include:
      - common
      - desktop
